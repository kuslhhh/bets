{
  "buildCommand": "cd server && bun prisma migrate deploy && bun prisma generate && bun run typecheck && cd ../client && bun run build",
  "outputDirectory": "client/dist",
  "installCommand": "bun install",
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/index.html" }]
}
