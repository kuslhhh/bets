{
  "buildCommand": "cd server && bun prisma generate && cd ../client && bun run build",
  "outputDirectory": "client/dist",
  "installCommand": "bun install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
